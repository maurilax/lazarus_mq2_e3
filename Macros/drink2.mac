#turbo
Sub Main


	:Loop
		/if (${Me.Skill[67]}>374) /goto :exiting
		/if (${FindItemCount[=Short Beer]} > 0 ) {
			/useitem Short Beer
		} else {
			/return
		}
	/delay 1s
	/goto :Loop
:exiting
/return
